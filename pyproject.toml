[project]
name = "picochat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "einops>=0.8.2",
    "lightning>=2.6.5",
    "rustbpe>=0.1.0",
    "tiktoken>=0.13.0",
    "torch>=2.12.1",
]


[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
